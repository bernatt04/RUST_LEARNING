learnt today:

- started `BlackJack` project to learn how to structure applications across multiple files
- learnt how to split a project into modules and import them using `mod` and `use crate::...`
- used `enum` to define custom types that map to specific variants (like `Suit` and `Rank` for cards)
- created `struct` definitions to group related information together (like `Card` containing a suit and rank, or `Deck` containing a `Vec<Card>`)
- used `impl` blocks to implement methods and constructors (like `new()`) for structs and enums
- used the `match` control flow operator to cleanly handle enum variants and assign values conditionally

files modified:

- projects/BlackJack/src/main.rs
- projects/BlackJack/src/card.rs
- projects/BlackJack/src/deck.rs
- projects/BlackJack/src/README.md

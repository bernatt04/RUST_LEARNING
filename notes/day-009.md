learnt today:

- reinforce principles by recreatring `card.rs` from scratch without referencing the original file
- understood the difference between associated functions (like `new()`) and methods using `&self`
- mastered the logic of `struct` instantiation: connecting function parameters to struct fields
- understood the "Field Init Shorthand" in Rust (when variables and fields have the same name)
- practiced using `match` to convert enum variants into numerical values
- resolved a project-wide bug where the IDE wasn't reporting errors due to case-sensitivity in `Cargo.toml`
- started implementing logic in `game.rs` for hand management

files modified:

- projects/BlackJack/src/card2.rs
- projects/BlackJack/src/game.rs
- projects/BlackJack/src/main.rs
- projects/BlackJack/Cargo.toml (root workspace)
- notes/day-009.md

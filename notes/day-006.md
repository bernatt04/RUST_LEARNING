learnt today:

- started `notes-manager` project
- taking user input from the console using `io::stdin().read_line(&mut var)`
- using `.expect("...")` to handle potential errors (similar to handling exceptions in other languages)
- converting strings into integers (parsing) using `.trim().parse().unwrap()`
- using the `format!` macro to build strings dynamically (like adding `.txt` to the file name)
- listing all files in a directory using `fs::read_dir(".")` and iterating through the entries


files modified:

- projects/notes-manager/src/main.rs

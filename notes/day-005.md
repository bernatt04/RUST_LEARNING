learnt today:

- file management using `std::fs` and `std::io`
- `File::create` to create a new file
- `.write_all(b"...")` to write content, using `b` to convert text into a byte array
- `fs::read_to_string` to easily read the entire contents of a file into a variable
- used `.unwrap()` to handle potential errors (it crashes the program if something goes wrong, like the file not existing)



files modified:

- exercises/file-manager/src/main.rs

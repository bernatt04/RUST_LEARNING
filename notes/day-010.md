learnt today:

- rand crate changes: thread_rng() is now rng() and gen_range() is now random_range() in 0.10.0
- distinction between module and object: deck::Deck::new() since the struct is inside the file
- use &self to read and &mut self to modify the object
- methods are called with variable.method() but need the trait in scope (like Rng)
- used pop() and unwrap() to deal cards from the deck vector
- finished the Blackjack game logic even though it looks bad as hell shawty (not all game cases are covered but you get the idea).
- it works so i'm calling it a win and moving to the next project

files modified:

- projects/BlackJack/src/main.rs
- projects/BlackJack/src/deck.rs
- projects/BlackJack/src/card.rs
- projects/BlackJack/Cargo.toml
- notes/day-010.md

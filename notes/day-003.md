learnt today:

- declared functions with explicit parameter types and return types (using `->`)
- big lesson on Ownership and memory: understood the difference between `String` (owned, dynamic memory) and `&str` (borrowed, fixed length)
- learnt that string concatenation using `+` requires taking ownership of the first `String` and borrowing the second (`+ &b`)
- used `.to_string()` to convert string literals (`&str`) into owned `String` types to pass them to functions
- experimented with floating-point numbers (`f32`)

files modified:

- exercises/functions/src/main.rs
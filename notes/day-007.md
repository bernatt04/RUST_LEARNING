learnt today:

- continued and finished the `notes-manager` project from day 6
- implemented cases 2, 3, and 4
- case 2: opening and reading file contents using `File::open` and `.read_to_string(&mut content)`
- using `Vec<String>` to store a list of items (file names) dynamically with `Vec::new()` and `.push()`
- iterating over directory entries, handling `entry.file_name().to_string_lossy()` to get the file name as a string
- case 3: deleting files using `fs::remove_file(filename)`
- case 4: exiting the program gracefully using `std::process::exit(0)`

files modified:

- projects/notes-manager/src/main.rs
